import { EuroToDollarPipe } from './euro-to-dollar-pipe';

describe('EuroToDollarPipe', () => {
  it('create an instance', () => {
    const pipe = new EuroToDollarPipe();
    expect(pipe).toBeTruthy();
  });
});
