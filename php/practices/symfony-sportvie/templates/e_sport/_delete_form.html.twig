<form method="post" action="{{ path('app_e_sport_delete', {'id': e_sport.id}) }}"
	  onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ e_sport.id) }}">
	<button class="btn">Delete</button>
</form>
