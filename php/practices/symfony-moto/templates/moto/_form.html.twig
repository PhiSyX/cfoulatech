{{ form_start(form) }}
    {{ form_widget(form) }}
    <div>
        <button class="btn btn-full" type="submit" title="Editer">
            {{ button_label|default('Enregistrer') }}
        </button>
    </div>
{{ form_end(form) }}
