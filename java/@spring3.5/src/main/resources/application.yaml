spring:
    application:
        name: CourseApplication

    datasource:
        url: jdbc:postgresql://localhost:5432/spring_course
        username: postgres
        password: WebA2025
        driver-class-name: org.postgresql.Driver

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true
server:
    port: 8080
    error:
        include-message: always

logging:
    level:
        be.cfoulatech.course: DEBUG
        org.hibernate.sql: DEBUG

debug: true
